<%-# Did this differently from the checkpoint, since voting is something that would draw a new user in to the site. -%>
<div class="vote-arrows pull-left">
  <% if current_user %>
    <div><%= link_to " ", post_up_vote_path(post), class: "glyphicon glyphicon-chevron-up" %></div>
  <% end %>
  <div><strong><%= post.points %></strong></div>
  <% if current_user %>
    <div><%= link_to " ", post_down_vote_path(post), class: "glyphicon glyphicon-chevron-down" %></div>
  <% end %>
</div>